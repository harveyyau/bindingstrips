# ============================================================================
# GUITAR BINDING GENERATOR - .gitattributes
# ============================================================================
# Git LFS configuration for large binary files (example STLs)

# Track example STL files with Git LFS (large binary files)
examples/*.stl filter=lfs diff=lfs merge=lfs -text
examples/**/*.stl filter=lfs diff=lfs merge=lfs -text

# .3MF files (also binary, but typically generated not committed)
# Uncomment if you want to track .3mf examples:
# examples/*.3mf filter=lfs diff=lfs merge=lfs -text
# examples/**/*.3mf filter=lfs diff=lfs merge=lfs -text

# Ensure text files have correct line endings
*.md text eol=lf
*.scad text eol=lf
*.sh text eol=lf
*.py text eol=lf

# Ensure shell scripts remain executable
*.sh text eol=lf
*.py text eol=lf
